version: "2"

sql:
  - engine: "postgresql"
    queries: "pkg/db/sql"
    schema: "migrations"
    gen:
      go:
        package: "gen"
        out: "pkg/db/gen"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true